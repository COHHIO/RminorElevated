# Update datatable options

Update datatable options

## Usage

``` r
datatable_options_update(x, options, hide_cols)
```

## Arguments

- x:

  `(datatables)`

- options:

  `(list)` of options to replace

## Value

`(datatables)`
